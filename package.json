{
  "name": "mission-japan-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --testPathPattern=test/"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
